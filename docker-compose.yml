services:
  proxy:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      - TARGET_DOMAIN=${TARGET_DOMAIN:-https://example.com}
      - PORT=${PORT:-3000}
    restart: unless-stopped
